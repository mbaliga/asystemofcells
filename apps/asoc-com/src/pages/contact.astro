---
import SimplePageLayout from '../layouts/SimplePageLayout.astro'

const EMAIL = 'nooz@asystemofcells.com'
---
<SimplePageLayout
  title="Contact: asystemofcells"
  description="Get in touch about asystemofcells apps - bugs, feature requests, or anything else."
  canonical="https://asystemofcells.com/contact"
>
  <p class="eyebrow">Contact</p>
  <h1>Get in touch.</h1>

  {EMAIL ? (
    <p>
      {/* Google Play's News policy reviewer must see the address in the raw
          HTML. Cloudflare's Email Address Obfuscation was rewriting it into a
          JS-decoded "[email protected]" placeholder, which reads as NO contact
          info to any client that doesn't run that script -- these email_off
          comments are Cloudflare's own documented opt-out for a region.
          Plain HTML comments, NOT Fragment set:html with a quoted attribute:
          that form HTML-escapes the string, which both broke the opt-out and
          printed the marker as visible text on the page. The static privacy
          pages use these same verbatim comments and verifiably reach
          production intact. */}
      <!--email_off-->
      Bug reports, feature requests, or anything else - email
      <a class="inline-link" href={`mailto:${EMAIL}`}> {EMAIL}</a>.
      <!--/email_off-->
    </p>
  ) : (
    <p>Contact details are being finalized. Check back shortly, or find the maker at <a class="inline-link" href="https://mdhv.xyz" target="_blank" rel="noopener noreferrer">mdhv.xyz</a> in the meantime.</p>
  )}
</SimplePageLayout>
